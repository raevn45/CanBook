import "../../styles/menu-controls.css";
import { AnimatePresence, motion } from "motion/react";
import { useEffect, useState } from "react";
import { Check, EyeOff, Plus, Trash2, UtensilsCrossed, X } from "lucide-react";
import { canteenapi } from "../../api";
const emptyForm={item_name:"",description:"",category:"meals",price:""};
export default function MenuManagement(){
 const [items,setItems]=useState([]),[form,setForm]=useState(emptyForm),[showAdd,setShowAdd]=useState(false),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[busyId,setBusyId]=useState(null);
 const load=async()=>{setLoading(true);try{const data=await canteenapi.menu();setItems(data.items||[]);setError("")}catch(err){setError(err.message)}finally{setLoading(false)}};
 useEffect(()=>{load()},[]);
 const toggle=async item=>{setBusyId(item.item_id);setError("");try{await canteenapi.toggleitem(item.item_id,!item.available);await load()}catch(err){setError(err.message)}finally{setBusyId(null)}};
 const remove=async item=>{if(!window.confirm(`Delete “${item.item_name}” permanently? This cannot be undone.`))return;setBusyId(item.item_id);setError("");try{await canteenapi.deleteitem(item.item_id);await load()}catch(err){setError(err.message)}finally{setBusyId(null)}};
 const submit=async e=>{e.preventDefault();setSaving(true);setError("");try{await canteenapi.additem({...form,price:Number(form.price)});setForm(emptyForm);setShowAdd(false);await load()}catch(err){setError(err.message)}finally{setSaving(false)}};
 const active=items.filter(i=>i.available),hidden=items.filter(i=>!i.available);
 return <main className="app-page canteen-menu-page menu-management-page"><header className="canteen-section-head"><div><span className="micro-label"><UtensilsCrossed size={13}/> CANTEEN / MENU CONTROL</span><h1>What's <em>on?</em></h1><p>The live menu students see. Hide an item when you run out, or delete an unused item permanently.</p></div><motion.button className="giant-submit menu-add-button" onClick={()=>setShowAdd(true)} whileHover={{y:-4}} whileTap={{scale:.97}}><Plus size={20}/> Add item</motion.button></header>{error&&<div className="form-error menu-error">{error}</div>}<section className="menu-control-summary"><motion.div whileHover={{y:-4}}><span>LIVE ITEMS</span><strong>{active.length}</strong></motion.div><motion.div whileHover={{y:-4}}><span>HIDDEN ITEMS</span><strong>{hidden.length}</strong></motion.div><motion.div whileHover={{y:-4}}><span>STAFF CONTROL</span><strong>HIDE / DELETE</strong></motion.div></section>{loading?<div className="loading-card">Loading menu…</div>:<motion.div className="canteen-menu-list" layout><AnimatePresence>{items.map((item,index)=><motion.article layout key={item.item_id} className={`canteen-menu-row ${item.available?"":"is-hidden"}`} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} exit={{opacity:0,scale:.96}} whileHover={{y:-4}}><span className="menu-row-number">{String(index+1).padStart(2,"0")}</span><div className="menu-row-copy"><small>{item.category}</small><h2>{item.item_name}</h2><p>{item.description||"No description yet."}</p></div><strong className="menu-row-price">AED {Number(item.price).toFixed(2)}</strong><div className="menu-row-actions"><motion.button className={`menu-remove ${item.available?"":"restore"}`} onClick={()=>toggle(item)} disabled={busyId===item.item_id} whileTap={{scale:.94}}>{item.available?<><EyeOff size={15}/> Hide</>:<><Check size={15}/> Show</>}</motion.button><motion.button className="menu-delete" onClick={()=>remove(item)} disabled={busyId===item.item_id} whileTap={{scale:.94}}><Trash2 size={15}/> Delete</motion.button></div></motion.article>)}</AnimatePresence></motion.div>}<AnimatePresence>{showAdd&&<motion.div className="menu-modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={e=>e.target===e.currentTarget&&setShowAdd(false)}><motion.form className="menu-modal" onSubmit={submit} initial={{opacity:0,y:30,scale:.96}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20,scale:.96}} transition={{type:"spring",stiffness:260,damping:24}}><button type="button" className="modal-close" onClick={()=>setShowAdd(false)} aria-label="Close"><X size={19}/></button><span className="micro-label">CANTEEN / NEW ITEM</span><h2>Add to the menu.</h2><p>Students will see this item immediately after you save it.</p><label>Item name<input required value={form.item_name} onChange={e=>setForm({...form,item_name:e.target.value})} placeholder="Chicken Sandwich"/></label><label>Description<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Freshly prepared…"/></label><div className="menu-form-grid"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option value="meals">Meals</option><option value="snacks">Snacks</option><option value="sandwiches">Sandwiches</option><option value="desserts">Desserts</option><option value="drinks">Drinks</option></select></label><label>Price (AED)<input required min="0" step="0.01" type="number" value={form.price} onChange={e=>setForm({...form,price:e.target.value})} placeholder="7"/></label></div><motion.button className="giant-submit" disabled={saving} whileHover={{y:-3}} whileTap={{scale:.98}}>{saving?"Saving…":"Add item"}<Plus size={19}/></motion.button></motion.form></motion.div>}</AnimatePresence></main>;
}
